pipeline{
  agent any
  stages{
    stage('build'){
      steps{
        sh '''
        
        echo "Build is running on host `hostname`"
        bash ${WORKSPACEn}/linux/scripting/shell/for.sh

        '''
      }
    }
  }
}
